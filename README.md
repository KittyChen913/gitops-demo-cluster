# gitops-demo-cluster
Infrastructure as Code (Terraform) for provisioning Kubernetes cluster used in GitOps demo architecture. Serves as the foundation layer where the Kubernetes environment is created before GitOps is applied.
