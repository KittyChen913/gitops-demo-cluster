# Rename to backend.tf after configuring remote state.
#
# terraform {
#   backend "s3" {
#     bucket         = "your-terraform-state"
#     key            = "gitops-demo-cluster/prod/terraform.tfstate"
#     region         = "us-east-1"
#     encrypt        = true
#     dynamodb_table = "terraform-locks"
#   }
# }
