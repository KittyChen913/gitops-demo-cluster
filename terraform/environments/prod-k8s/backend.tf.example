# 前置條件：prod/ 的 terraform apply 已完成且 remote state 存於 S3。

terraform {
  backend "s3" {
    bucket  = "your-aws-tf-state-bucket"
    key     = "gitops-demo-cluster/prod-k8s/terraform.tfstate"
    region  = "ap-southeast-1"
    encrypt = true

    # dynamodb_table = "terraform-locks"
  }
}
